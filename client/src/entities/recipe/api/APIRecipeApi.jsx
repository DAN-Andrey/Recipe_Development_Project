class InternetRecipeApi {
  static async searchRecipes(query) {
    // запрос к TheMealDB или другому сервису
  }
  
  static async getRecipeById(id) {
    // получить подробности одного рецепта
  }
}